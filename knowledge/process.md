# Process lessons for /retro — things about the harness itself (router, gates, prompts)
<!-- - YYYY-MM-DD TICKET: what misfired and what you would change -->
- 2026-09-24 APR-7557: implementer (isolation: worktree, 60 turns) stranded 11 uncommitted files in a temp worktree after a 52-min stalled call; fixes: no isolation, commit per green step, API_TIMEOUT_MS 5 min, review-plan never on opus, briefs are the contract.
